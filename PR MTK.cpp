#include <iostream>
using namespace std;
int main (){
    float a, b, c, d, hasil1, hasil2, hasil3;
    char x; 
    
    cin >> x >> a >> x >> b >> x >> x >> x >> c >> x >> d >> x;
    hasil1 = (a+b)*(c-d);
    
    cin >> x >> a >> x >> b >> x >> x >> x >> c >> x >> d >> x;
    hasil2 = (a+b)*(c-d);
    
    cin >> x >> a >> x >> b >> x >> x >> x >> c >> x >> d >> x;
    hasil3 = (a+b)*(c-d);
    
    cout << hasil1 << " " << hasil2 << " " << hasil3; 
   
    
    return 0;
}
